1. Functions in JS : To Be Or Not To Be

var expect = function(val) {

    return {
        toBe: function (expected) {
            if (val !== expected) {
                throw new Error('Not Equal');
            }
            return true;
        },
        notToBe: function (expected) {
            if (val === expected) {
                throw new Error('Equal');
            }
            return true;
        },
    };

};


2. Function: Counter II

var createCounter = function(init) {
    let value = init;
    return {
        increment: function(){
        return value +=1;
        },
        reset: function(){
            return value= init;
        },

        decrement: function(){
            return value -=1;
        },
        
    }
    
};

